{
  "name": "@nikon-camera-bridge/desktop",
  "version": "0.3.0",
  "description": "Electron desktop shell for Nikon Camera Bridge",
  "homepage": "https://github.com/AMDphreak/nikon-camera-bridge",
  "repository": {
    "type": "git",
    "url": "https://github.com/AMDphreak/nikon-camera-bridge.git"
  },
  "type": "module",
  "main": "out/main/index.js",
  "scripts": {
    "dev": "electron-vite dev",
    "build": "electron-vite build",
    "build:win": "electron-vite build && electron-builder --win msi --x64 --publish never --config.directories.output=release/win-x64-msi && electron-builder --win msi --arm64 --publish never --config.directories.output=release/win-arm64-msi && electron-builder --win zip --x64 --publish never --config.directories.output=release/win-x64-zip && electron-builder --win zip --arm64 --publish never --config.directories.output=release/win-arm64-zip",
    "build:linux": "electron-vite build && electron-builder --linux deb --x64 --publish never --config.directories.output=release/linux-x64-deb && electron-builder --linux deb --arm64 --publish never --config.directories.output=release/linux-arm64-deb && electron-builder --linux AppImage --x64 --publish never --config.directories.output=release/linux-x64-appimage && electron-builder --linux AppImage --arm64 --publish never --config.directories.output=release/linux-arm64-appimage && electron-builder --linux flatpak --x64 --publish never --config.directories.output=release/linux-x64-flatpak && electron-builder --linux flatpak --arm64 --publish never --config.directories.output=release/linux-arm64-flatpak",
    "build:mac": "electron-vite build && electron-builder --mac dmg --x64 --publish never --config.directories.output=release/mac-x64-dmg && electron-builder --mac dmg --arm64 --publish never --config.directories.output=release/mac-arm64-dmg",
    "preview": "electron-vite preview",
    "typecheck": "tsc -p tsconfig.node.json --noEmit && tsc -p tsconfig.web.json --noEmit"
  },
  "dependencies": {
    "@nikon-camera-bridge/core": "workspace:*",
    "@nikon-camera-bridge/video": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "electron": "^33.2.1",
    "electron-builder": "^25.1.8",
    "electron-vite": "^2.3.0",
    "typescript": "~5.7.2",
    "vite": "^5.4.14"
  },
  "author": "AMDphreak",
  "license": "MIT",
  "build": {
    "electronVersion": "33.4.11",
    "appId": "com.amdphreak.nikonCameraBridge",
    "productName": "Nikon Camera Bridge",
    "artifactName": "Nikon.Camera.Bridge-${version}-${os}-${arch}.${ext}",
    "directories": {
      "output": "release",
      "buildResources": "build"
    },
    "files": ["out/**/*"],
    "win": {
      "target": ["msi", "zip"],
      "signAndEditExecutable": false
    },
    "linux": {
      "target": ["deb", "AppImage", "flatpak"],
      "category": "Utility",
      "maintainer": "AMDphreak",
      "synopsis": "Nikon USB webcam bridge and control shell"
    },
    "flatpak": {
      "runtime": "org.freedesktop.Platform",
      "sdk": "org.freedesktop.Sdk",
      "runtimeVersion": "25.08",
      "base": "org.electronjs.Electron2.BaseApp",
      "baseVersion": "25.08",
      "finishArgs": [
        "--socket=wayland",
        "--socket=x11",
        "--share=ipc",
        "--device=dri",
        "--socket=pulseaudio",
        "--filesystem=home",
        "--share=network",
        "--talk-name=org.freedesktop.Notifications"
      ]
    },
    "mac": {
      "target": ["dmg"],
      "category": "public.app-category.utilities",
      "hardenedRuntime": false,
      "gatekeeperAssess": false
    },
    "forceCodeSigning": false
  }
}
